// simple script for progressive enhancements (optional)
// currently no heavy JS is required because forms post server-side.
// but we include a tiny helper to confirm deletes (already inlined), and can later be extended.

console.log('Thai Restaurant Admin UI loaded');
